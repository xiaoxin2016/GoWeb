# 所有文本文件统一以 LF 存储并检出，覆盖客户端的 core.autocrlf 设置。
# 防止 Windows 上检出的 install.sh 等脚本带 CRLF 后拷到 Linux 无法执行。
* text=auto eol=lf

# 明确关键类型（冗余声明，双保险）
*.sh   text eol=lf
*.go   text eol=lf
*.md   text eol=lf
*.html text eol=lf
*.css  text eol=lf
*.js   text eol=lf
*.json text eol=lf
Dockerfile text eol=lf

# 二进制类型不做任何转换
*.png  binary
*.jpg  binary
*.gz   binary
*.zip  binary
